mvn clean install -DskipTests

Plugin:
----------
mvn <plugin>:<goal>

mvn <PLUGIN>:help

mvn jaxb2:xjc

mvn jaxb2:help -Ddetail=true -Dgoal=<goal-name>
mvn jaxb2:help -Ddetail=true -Dgoal=xjc

mvn help:describe -Dcmd=compile

Multi module project setup:
--------------------------
root-fokder:
mvn archetype:generate -DgroupId="com.project" -DartifactId=fullstack-parent -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false
inside frontend:
mvn archetype:generate -DgroupId=com.example -DartifactId=backend -DarchetypeArtifactId=maven-archetype-quickstart -DinteractiveMode=false


Java Setting:
----------------
Spring boot:
<java.version>11<java.version>

others:
<maven.compiler.source>11</maven.compiler.source>
<maven.compiler.target>11</maven.compiler.target>

or
maven-compiler-plugin

